# Email account (IMAP)
EMAIL_HOST=imap.gmail.com
EMAIL_PORT=993
EMAIL_USER=derious.v@ecustomsolutions.com
EMAIL_PASS=ruwu dkgs ykxx ebfz

# Gmail labels (or IMAP folders)
EMAIL_LABEL_INBOX=parity-checker/inbox
EMAIL_LABEL_PROCESSED=parity-checker/processed

# Only accept from these senders (comma-separated, lowercase emails)
EMAIL_SENDER_ALLOWLIST=astrid@aloware.com

# Optional filters
# SUBJECT_REGEX=^Crystal's accounts ALL phone numbers
# ATTACHMENT_NAME_REGEX=\.csv$

# Path to parity-checker project root (absolute)
PARITY_ROOT=/Users/k2duser/WebstormProjects/callpurity-api-testing/parity-checker

# Optional: override API base for parity runs (falls back to parity-checker/.env)
API_BASE_URL=https://api-lab.callpurity.com/latest

# Optional weekly caps for apply (or leave defaults)
# MAX_ADD=2000
# MAX_DELETE=2000


